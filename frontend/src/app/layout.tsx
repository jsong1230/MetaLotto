// next-intl을 위한 최소 root layout
// 실제 UI layout은 src/app/[locale]/layout.tsx에서 처리
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
